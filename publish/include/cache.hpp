#ifndef CACHE
#define CACHE

class CacheProvider {
public:
    /*
    void put_batch(symbol_id, expiry_id, slice_view);
    void get(contract_id);
    void get_surface(symbol_id, expiry_id);
    */
};

#endif